
library(readxl)

gene_data <- read_excel("D:/1st chapter/Molecular study/qPCR/Gene number2.xlsx")

  pivot_longer(cols = starts_with("XX"):ends_with("XX"),
  mutate(Environment = factor(Environment, levels = c("XX", "XX", "XX")))

ggplot(gene_data_long, aes(x = Env, y = value, fill = XX)) +
  geom_boxplot() +
  facet_wrap(~ XX, scales = "free_y") +
  labs(x = "Env", y = "XX", fill = "XX") +
  theme_minimal()


